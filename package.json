{
  "name": "transferhub",
  "version": "1.0.0",
  "description": "TransferHub serverless data movement API",
  "main": "dist/src/api/handler.js",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "sam:build": "sam build",
    "sam:deploy": "sam deploy --guided"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.840.0",
    "@aws-sdk/client-s3": "^3.840.0",
    "@aws-sdk/lib-dynamodb": "^3.840.0",
    "@aws-sdk/s3-request-presigner": "^3.840.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.25.6",
    "jest": "^30.0.5",
    "ts-jest": "^29.4.0",
    "typescript": "^5.8.3"
  }
}
